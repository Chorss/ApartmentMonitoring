spring:
  application:
    name: apartment-monitoring
  cloud:
    config:
      uri: http://localhost:8888
      username: username
      password: password
      enabled: true

  profiles:
    active: dev